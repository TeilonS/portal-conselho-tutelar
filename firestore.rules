rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // FAQ — leitura pública, escrita bloqueada (será liberada para admins na Fase 5)
    match /faq/{documento} {
      allow read: if true;
      allow write: if false;
    }

    // Notícias — leitura pública, escrita bloqueada (será liberada para admins na Fase 5)
    match /noticias/{documento} {
      allow read: if true;
      allow write: if false;
    }

    // Bloqueia qualquer outra coleção por padrão
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
